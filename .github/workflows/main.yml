name: Keep Render Server Awake

on:
  schedule:
    - cron: "*/10 * * * *" # 매 10분마다
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Render Server
        run: |
          curl --silent --output /dev/null https://algovote.onrender.com/
